#ifndef __QPCLUSTERING__
#define __QPCLUSTERING__ 1

/// Placeholder for the cluster-reconstruction algorithm. Not implemented;
/// the constructor is declared but has no definition (no QPClustering.cpp),
/// so instantiating this class fails at link time.
class QPClustering{
    public:
    QPClustering();
    protected:
    private:
};

#endif
