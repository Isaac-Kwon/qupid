#ifndef __QPCLUSTER__
#define __QPCLUSTER__

/// Placeholder for cluster data. Cluster reconstruction is not implemented;
/// the constructor is declared but has no definition (no QPCluster.cpp), so
/// instantiating this class fails at link time.
class QPCluster{
    public:
    QPCluster();
    protected:
    private:
};

#endif